import React from 'react';
import { Navbar, Toolbar } from 'sancho';

const Menu = () => {
    return (
        <Navbar>
            <Toolbar>HELLO WORLD</Toolbar>
        </Navbar>
    );

}

export default Menu;